<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
<item android:state_checked="true">
    <shape
    android:shape="rectangle"
    android:visible="true"
    android:dither="true"
    android:useLevel="false">
    <gradient
        android:startColor="@color/color_switch"
        android:endColor="@color/color_switch"
        android:angle="270"/>
    <corners
        android:radius="25dp"/>
    <size
        android:width="100dp"
        android:height="30dp" />
    </shape>
</item>
    <item android:state_checked="false">
        <shape
                android:shape="rectangle"
                android:visible="true"
                android:dither="true"
                android:useLevel="false">
            <gradient
                    android:startColor="@color/gray_border"
                    android:endColor="@color/gray_border"
                    android:angle="270"/>
            <corners
                    android:radius="25dp"/>
            <size
                    android:width="100dp"
                    android:height="30dp" />
        </shape>
    </item>
</selector>